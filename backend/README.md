# shrinkflation (backend)

Python package for the Shrinkflation Detector: Kroger client, size parser, pipeline, API, and the `shrink` CLI.
Setup, commands, and design notes are in the repository root `README.md` and `AGENTS.md`.
